import Login from "@/components/Login/Login";
export const metadata = {
    title: "Login",
    description: "This is the login page",
};

const page = () => {
    
    return (
        <>
            <Login />
        </>
    );
};

export default page;